def function main with parameters() {
    def var x = 2 ^ 3
    print x

    print call sum_of_0_to_n_powered_to_k with parameters (x, enter k)
}

def function fibonacci with parameters(n) {
    if n == 0 {
        return
    }
    else {
        call fibonacci with parameters (n - 1)
    }
    print n
}

def function sum_of_0_to_n_powered_to_k with parameters(n, k) {
    if n == 0 {
        return n
    }

    def var result = n ^ k
    return result + call sum_of_0_to_n_powered_to_k with parameters(n - 1, k)
}